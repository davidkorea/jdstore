

<div class="col-md-12">
  <div class="product">
    <%= link_to("Edit", edit_admin_product_path(@product), class: "btn btn-primary pull-right")%>
  </div>
  <h2>Title:<%= @product.title %></h2>
  <p>Description:<%= @product.description %></p>
  <p>Quantity:<%= @product.quantity %></p>
  <p>Price:<%= @product.price %></p>
</div>
